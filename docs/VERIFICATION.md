# 发布验证记录

验证日期：2026-09-13。本记录对应本次公开源码，不代表真实业务部署或模型效果验收。

| 检查 | 实际结果 |
| --- | --- |
| Maven 工程结构 | 33 个 POM 文件可解析，离线工程校验通过 |
| 后端编译打包 | `mvn -o -DskipTests -Dmaven.test.skip=true package` 通过，33 个模块均为 SUCCESS；没有运行外部服务集成测试 |
| 前端依赖 | 使用锁文件恢复依赖，安装脚本关闭；缺失的公共依赖通过公开仓库补齐 |
| 前端生产构建 | `pnpm build` 成功生成 dist |
| 配置和数据库 | YAML 可解析；88 张表仅保留结构，没有 INSERT、业务记录或账号种子 |
| 隐私检查 | 最终源码中的已知个人身份、私有服务地址和密钥检查未发现待处理项 |
| 凭据扫描 | Gitleaks 扫描为 0；仅对两个公开的 Stable Diffusion sampler 枚举值设置精确例外 |
| 独立复核 | 环境变量、数据库结构、安全工具和许可证经第二次只读检查，无已知静态发布阻塞 |

构建产生了 Sass、Vite CJS、Browserslist 等旧依赖提示及 Maven 重复依赖声明警告，没有将这些警告描述为全部消除。没有单独运行完整前端严格类型检查。

未连接原 MySQL、Redis、对象存储或模型服务，也没有执行真实用户数据的端到端测试。空库不含管理员、角色菜单和业务种子，需要自行初始化。外部 AI 适配器默认关闭，使用者需按配置说明实现服务端授权和接口。

原始素材已替换为中性占位资源；构建所需的演示音效使用新生成的静音 WAV。界面存在不表示继承框架的每个可选模块都已完成业务验收。演示与获奖材料的范围分别见 PROJECT.md 和 AWARDS.md。